/**
 * ============================================================
 * e2e 测试库路径的唯一来源
 * ============================================================
 * playwright.config.ts 与各个直接连库的规格都从这里取路径，
 * 避免出现「配置一套、规格里又各写一份」而对不上。
 *
 * 为什么用**稳定文件名**而不是「每轮一个随机名」：试过后者，结论是
 * playwright.config.ts 会在不止一个进程里被求值，随 runId 变化的路径
 * 会让服务端与规格连到不同的库（实测出现两个 k-e2e-*.db）。
 * 改为稳定名 + 配置侧「尽力删除」（见 playwright.config.ts 注释）。
 */

import path from 'path';

export const DB_PATH = path.resolve(__dirname, '..', 'e2e', '.tmp', 'k-e2e.db');

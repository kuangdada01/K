# 版本号单一来源（由 native/build.gradle 读取；发版时改这里）
#
# ⚠️ 两处必须同步改，否则 App 内更新提示会指向错误的版本：
#   · .env 的 APP_VERSION / APP_APK_URL / APP_UPDATE_NOTES（服务端 /api/app/version 的数据源）
#   · 服务器上实际的 APK 文件名（deploy.ps1 依赖 k-app-<versionName>-release.apk 这个命名）
versionCode=1
versionName=0.1.0
